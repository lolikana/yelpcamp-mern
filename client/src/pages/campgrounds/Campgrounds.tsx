import ReadAll from '@components/campgrounds/ReadAll';

const Campgrounds = () => {
  return (
    <article className={'page-article'}>
      <ReadAll />
    </article>
  );
};

export default Campgrounds;
